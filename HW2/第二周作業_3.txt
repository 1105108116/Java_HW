1.不同，char輸出為ASCLL碼為88的英文字元X。若要使char輸出數字88，則需要再輸出的時候做型態轉換。（即：System.out.println("Char = " + (int)b);）
2.String變數輸出為881，即在原字串後加上整數1，char變數則無法直接進行整數運算，需經過型態轉換。若令char變數的資料直接加1使其ASCLL內碼變成89，則輸出為英文字母Y，若欲使char變數輸出數字89，則使用上題之方法。